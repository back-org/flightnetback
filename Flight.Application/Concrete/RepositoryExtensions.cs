namespace Flight.Application.Concrete;

/// <summary>
/// Classe de réserve pour les extensions du dépôt générique.
/// Ajoutez ici des méthodes d'extension pour les requêtes avancées
/// (ex : pagination, filtres globaux, soft-delete automatique).
/// </summary>
public static class RepositoryExtensions
{
    // Les extensions de requête seront ajoutées ici selon les besoins.
    // Exemple :
    // public static IQueryable<T> ExcludeDeleted<T>(this IQueryable<T> query)
    //     where T : IDeleteEntity
    //     => query.Where(e => !e.IsDeleted);
}
